# Fixtures for User model tests.
# Field names corrected to match schema: firstname, lastname, username.
# password_digest generated via BCrypt for use in authentication tests.

alice:
  email: alice@example.com
  password_digest: <%= BCrypt::Password.create('password123') %>
  firstname: Alice
  lastname: Smith
  username: alice_smith

bob:
  email: bob@example.com
  password_digest: <%= BCrypt::Password.create('password456') %>
  firstname: Bob
  lastname: Jones
  username: bob_jones
